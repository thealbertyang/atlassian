{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach (Current Window)",
      "type": "extensionHost",
      "request": "attach",
      "port": 9229,
      "outFiles": ["${workspaceFolder}/out/**/*.js"]
    },
    {
      "name": "Run Extension (HMR)",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/out/**/*.js"],
      "preLaunchTask": "ensure-compiled",
      "env": {}
    }
  ]
}
